# Training dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-train.txt

pytorch-lightning>=2.0
hydra-core>=1.3
omegaconf>=2.3
rich>=13.0
lightning-utilities>=0.9
h5py>=3.8                # only needed for the original HDF5 music pipeline
librosa>=0.10            # mel filterbanks for the fullness / clarity losses
lameenc>=1.5             # MP3 encoder for codec simulation, replaces apply_codec
tensorboard>=2.13        # default logger; swap for wandb in the config if preferred
tqdm>=4.65
